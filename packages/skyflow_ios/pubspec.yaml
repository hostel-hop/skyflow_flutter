name: skyflow_ios
description: Skyflow Ios client
version: 0.0.4
homepage: 'https://www.skyflow.com/'

environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  plugin:
    platforms:
      # android:
      #   package: com.skyflow.flutter
      #   pluginClass: SkyflowPlugin
      ios:
        pluginClass: SkyflowIosPlugin